# Contact form SMTP (Resend) — server-side only, never commit real values.
# Copy to .env and fill with real values.
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=re_xxxxxxxxxxxxxxxxxxxx
CONTACT_FROM=onboarding@resend.dev
CONTACT_TO=emmanuelberriojimenez13@gmail.com